import { fontFace } from '@vanilla-extract/css'

import * as font from './font'

export const EqualsBold = fontFace({
  src: `url(${font.EqualsBold})`
})

export const EqualsLight = fontFace({
  src: `url(${font.EqualsLight})`
})
